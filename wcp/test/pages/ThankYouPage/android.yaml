functions:

#--------------------------------------------------------------------------------------------------------------------

  # Description: Asserts the displayed Name and Address are correct.
  # Params:      ${expectedFulfillmentName}     [Mandatory]  Expected Name.
  #              ${expectedFulfillmentAddress}  [Mandatory]  Expected Name.
  - name: functions.thankYouPage.assertNameAndAddress
    platform: android
    flow:
      - executeFunction:
          name: functions.thankYouPage.assertFulfillmentName # <-- ${expectedFulfillmentName}
      - executeFunction:
          name: functions.thankYouPage.assertFulfillmentAddress # <-- ${expectedFulfillmentAddress}
      - log:
          message: "End function: functions.thankYouPage.assertNameAndAddress"
          color: BLUE

