var moneyBoxArray = JSON.parse(process.argv[2]);
var matchValue = process.argv[3];

  for (var i = 0; i < moneyBoxArray.length; i++) {
    for (var key in moneyBoxArray[i]) {
      if (key.includes(matchValue)) {
        return console.log(moneyBoxArray[i][key]);
      }
    }
  }