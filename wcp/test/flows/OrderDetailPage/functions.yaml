functions:
  #--------------------------------------------------------------------------------------------------------------------

  # Description: This flow Validate the reduced price and free shipping on money box box
  - name: functions.orderDetailPage.validateReducedPriceAndPickUpFreeShipping
    flow:
      - executeFunction:
          name: functions.orderDetailPage.scrollToPOS


    #Temporary workaround due to open defect: https://jira.walmart.com/browse/CEPG-232820
    #This issue has resolved, need to test this function to make sure it works.
      # - executeFunction:
      #     name: functions.orderDetailPage.validateReducedPrice 

      - executeFunction:
          name: functions.orderDetailPage.validatePickUpFreeShipping
          
            