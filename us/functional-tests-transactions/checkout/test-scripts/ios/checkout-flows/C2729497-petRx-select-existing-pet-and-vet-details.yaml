# Validate PetRx - Select existing Pet and Vet details in checkout flow

general:
  platform: ios
  tags: C2729497, prod-functional-checkout
  inherit:
    filesRunAll:
      - us-errors-helpers.yaml
  testCaseId: C2729497

scenarios:
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: End Before test

      # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: "R2_SUBFLOW_PRE_TRANSACTION"
  # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

  - name: Validate PetRx - Select existing Pet and Vet details in checkout flow
    endTestOnFailure: true
    flow:
      - log: Starting test "Validate PetRx - Select existing Pet and Vet details in checkout flow"

      # Navigate onboarding to home
      - executeFunction:
          name: us.functions.global.onboarding.navigateOnboardingToHome

      # Navigate to sign in page
      - executeFunction:
          name: us.functions.home.navigateHomeToSignIn

      # Sign in with default account
      - executeFunction:
          name: us.functions.global.authentication.signInAccount
          params:
            - name: email
              string: us.data.prod.checkout.bagfee.optin.ios.email
            - name: password
              string: us.data.prod.checkout.bagfee.optin.ios.password

      - executeFunction:
          name: us.functions.global.navigation.accountToHome

      #Enable dummy payment through debug panel
      - executeFunction:
          name: us.functions.functional.utils.placeOrder.enablePaymentByPass

      - executeFunction:
          name: us.functions.home.changeStore
          params:
              - name: zipcode
                string: 95122 
              - name: storeName
                string: San Jose Supercenter
      
      - executeFunction:
          name: us.functions.gic.changeFulfillment
          params:
            - name: option
              string: Shipping
      
      # Go to cart and remove items in cart
      - click:
          identifier: us.mappings.navBar.cartIcon

      - executeFunction:
          name: us.functions.cart.removeAllCartItems

      - executeFunction:
          name: us.functions.cart.removeUnavailableItemsFromCart

      - executeFunction:
          name: us.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: us.data.checkout.petrx1.itemlink

      - executeFunction:
          name: us.functions.item.petrxItem.verifyPresent

      - executeFunction:
          name: us.functions.item.addItemToCart

      - executeFunction:
          name: us.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: us.data.checkout.petrx2.itemlink

      - executeFunction:
          name: us.functions.verifyPetVetApprovalRequired
    
      - executeFunction:
          name: us.functions.item.addItemToCart

      # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: "R2_SUBFLOW_TRANSACTION"
      # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      - executeFunction:
          name: us.functions.global.navigation.toCartFromNavBar

      - executeFunction:
          name: us.functions.cart.verifyCardDetails
          params:
            - name: fulfillmentText
              string: us.data.fulfillment.shipping

      # Continue to checkout
      - executeFunction:
          name: us.functions.cart.continueToCheckout

      # Validate shipping module
      - executeFunction:
          name: us.functions.checkout.reviewOrder.verifyCardDetails
          params:
            - name: fulfillmentText
              string: Shipping    

      #  Add Pet and Vet details
      - executeFunction:
          name: us.functions.checkout.AddPetVet

      #Select existing pet and vet
      - executeFunction:
          name: us.functions.functional.checkout.reviewOrder.SelectPetVet
          params:
            - name: multipleItems
              string: true

      #Verifying PetRx details in review order page after selecting a pet and vet
      #Total number of PetRx items, adjusted by adding 1 since label indexing starts at 1. 
      #Actual PetRx items are 2.
      - executeFunction:
          name: us.functions.functional.checkout.reviewOrder.verifyPetRxCardDetails.after.selectingPetandVet
          params:
            - name: itemsCount
              string: 3
            - name: splitItems
              string: false        
              
      - storeIn:
           key: testStatus
           value: passed

  - name: After
    after: true
    useTestConfigErrors: false
    flow:
      - executeFunction:
          name: us.functions.utils.afterSteps
