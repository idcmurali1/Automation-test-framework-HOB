# Non-amendable
general:
  tags: C2696500, unified-teflon-e2e, PetRx, unified-p0-ecomm-teflon-e2e
  inherit:
    filesRunAll:
      - us-errors-helpers.yaml
  testCaseId: C2696500
  metadata:
    - name: scenarioId
      string: PV012

scenarios:

  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      #Get Environment Values
      - executeFunction:
          name: us.function.utils.getEnvironmentValue

      # Set Platfrom Values
      - executeFunction:
          name: functions.utils.setPlatformInfo
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

  - name: Main
    endTestOnFailure: true
    flow:

      - log: Start Main

      #Navigate onboarding to home
      - executeFunction:
          name: unified.us.functions.global.onboarding.navigateOnboardingToHome

      # Will take the user to the account menu from where the user can sign in to the app
      - executeFunction:
          name: unified.us.functions.home.navigateHomeToSignIn

      - executeFunction:
          name: unified.us.functions.authentication.signInInfo
          params:
            - name: android.email
              string: us.data.android.petrx.vetfood.email
            - name: android.password
              string: us.data.android.petrx.vetfood.password
            - name: ios.email
              string: us.data.ios.petrx.petVetDietFlow.email
            - name: ios.password
              string: us.data.teflon.password

      # Astro clear cart
      - executeFunction:
          name: us.functions.utils.astro.clearCart
          params:
            - name: custEmail
              string: ${email}

      # User signs in to the account
      - executeFunction:
          name: unified.us.functions.global.authentication.signInAccount
          params:
            - name: email
              string: ${email}
            - name: password
              string: ${password}

      - executeFunction:
          name: unified.us.functions.global.navigation.accountToHome

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      # # Add items to cart using deeplink
      - executeFunction:
          name: unified.us.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: us.data.teflon.petVetDiet.item
      - sleep:
          duration: 3000
          
      - executeFunction:
          name: unified.us.functions.verifyPetVetApprovalRequired

      - executeFunction:
          name: unified.us.functions.item.addItemToCart

      - executeFunction:
          name: unified.us.functions.global.navigation.toCartFromNavBar

      - executeFunction:
          name: unified.us.functions.verifyPetVetApprovalRequired

#     Continue to checkout
      - executeFunction:
          name: unified.us.functions.cart.continueToCheckout

# Add Pet and Vet details
      - executeFunction:
          name: unified.us.functions.checkout.AddPetVet
      - executeFunction:
          name: us.functions.checkout.SelectPetVet

      - executeFunction:
          name: unified.us.functions.home.closeWPlusPromotionPopUpIfDisplayed
  
      - executeFunction:
          name: unified.us.functions.checkout.placeOrder

#Validate order confirmation page
      - executeFunction:
          name: unified.us.functions.orderConfirmation.getOrderNumber

      - log: ${orderNo}

      - executeFunction:
          name: us.functions.utils.astro.petVetDiet.moveOrderStatus
          params:
            - name: payload
              string: us.data.teflon.petVetDiet.payload.orderMovement
            - name: orderNo
              string: ${orderNo}

      - log: "Waiting for order to status gets updated after the API call"
      - sleep:
          duration: 10000
              
      - executeFunction:
          name: unified.us.functions.petVetDiet.orderDeliveryVerification

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    flow:
      - executeFunction:
          name: unified.us.functions.utils.afterSteps
