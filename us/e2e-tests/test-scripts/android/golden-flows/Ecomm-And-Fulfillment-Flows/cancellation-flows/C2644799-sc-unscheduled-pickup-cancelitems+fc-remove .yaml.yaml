general:
  platform: android
  tags: C2644799, us-golden-flows, us-golden-flows-ecom-p2, p2-ecomm-teflon-e2e
  inherit:
    filesRunAll:
      - us-errors-helpers.yaml
  testCaseId: C2644799
  metadata:
    - name: scenarioId
      string: EC013

scenarios:

  - name: Before
    before: true
    endTestOnFailure: true
    flow:

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

#*******************************************************************************************************

      # Astro - Create order and move to PO Acknowledged

#*******************************************************************************************************

      # Fetch stock available items using fetch API
      - executeFunction:
          name: us.functions.utils.fetchFulcrumAvailableItem
          params:
            - name: source
              string: astro
            - name: itemType
              string: STORE_UNSCHEDULED_PICKUP
            - name: store
              string: 32144
            - name: backupItem
              string: 499312381
      - storeIn:
          key: item1
          value: $itemFetched

      # Fetch stock available items using fetch API
      - executeFunction:
          name: us.functions.utils.fetchFulcrumAvailableItem
          params:
            - name: source
              string: astro
            - name: itemType
              string: FC_DELIVERY
            - name: store
              string: 32144
            - name: backupItem
              string: 732383650
      - storeIn:
          key: item2
          value: $itemFetched
          
      - executeFunction:
          name: us.functions.utils.astro.createOrder
          params:
            - name: payload
              string: us.data.flow013.android.nonAmendablePayload 

  - name: Main
    testCaseId: C2096058
    flow:

      - log: Start Main

      # Navigate onboarding to home
      - executeFunction:
          name: us.functions.global.onboarding.navigateOnboardingToHome

      # Will take the user to the account menu from where the user can sign in to the app
      - executeFunction:
          name: us.functions.home.navigateHomeToSignIn

      # User signs in to the account
      - executeFunction:
          name: us.functions.global.authentication.signInAccount
          params:
            - name: email
              string: us.data.flow013aa.email
            - name: password
              string: us.data.teflon.password

      - goBack: true

      # Deeplink into order details page
      - executeFunction:
          name: us.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: walmart://orders/$orderNo

      # Verify "cancelOrder" flag
      # If cancelOrder == true, cancel order by cancel order button
      # If cancelOrder == false, cancel order by request cancellation button/remove item button
      # - executeFunction:
      #     name: us.functions.cancel.verifyCancelOrderStatus

      # Includes steps of scrolling to the end of the screen to Cancel Order option, tapping on it, selecting the reason and then tapping on Request Cancel order button
      - executeFunction:
          name: us.functions.cancel.orderItems
          params:
            - name: itemTypes
              string: sc,fc  
            - name: reasonIndex
              string: 5

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    flow:
      - executeFunction:
          name: us.functions.utils.afterSteps
