general:
  platform: android
  tags: C2656881, us-golden-flows, p1-ecomm-teflon-e2e, p1-fulcrum-teflon-e2e
  inherit:
    filesRunAll:
      - us-errors-helpers.yaml
  testCaseId: C2656881
  metadata:
    - name: scenarioId
      string: RT008
  #Covered as  part of 470
scenarios:

  - name: Before
    before: true
    endTestOnFailure: true
    flow:

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

 #***************************************************************************************************************

  # Astro - Create order 

 #***************************************************************************************************************

      - executeFunction:
          name: us.functions.utils.fetchFulcrumAvailableItem  #us.functions.utils.getSearchableItem
          params:
            - name: itemType
              string: FC_DELIVERY
            - name: store
              string: 32144
            - name: itemFilter
              string: buynow

      - storeIn:
          key: item1
          value: $itemFetched 
      - storeIn:
          key: fcItem
          value: $itemFetched

      # Create order for fast replacement
      - executeFunction:
          name: us.functions.utils.astro.createOrder
          params:
            - name: payload
              string: us.data.flow.replacement.createOrderPayload

      #Move FC items to delivered status
      - executeFunction:
          name: us.functions.utils.astro.getOrderStatus
          params:
            - name: orderNo
              string: ${orderNo}

      - executeFunction:
          name: us.functions.utils.astro.setOrderStatus
          params:
            - name: orderNo
              string: ${orderNo}
            - name: orderType
              string: FC_DELIVERY
            - name: status
              string: Delivered


  - name: Main
    flow:
      - log: Start Main

      # - storeIn:
      #     key: orderStatus
      #     value: Delivered

      # Navigate onboarding to home
      - executeFunction:
          name: us.functions.global.onboarding.navigateOnboardingToHome
  
      # Navigate to sign in page
      - executeFunction:
          name: us.functions.home.navigateHomeToSignIn

      # Account sign in
      - executeFunction:
          name: us.functions.global.authentication.signInAccount
          params:
            - name: email
              string: e2e.teflon.flow.firstscane@gmail.com
            - name: password
              string: us.data.flow037a.password
      
      # - executeNode:
      #     file: us/e2e-tests/helpers/getOrderhelper.js
      #     args:
      #       - value: $email
      #       - value: $orderStatus
      #     getResponse:
      #       storeIn: orderNo      
      # - log: $orderNo

         # Navigate to purchase history page
      - executeFunction:
          name: us.functions.global.navigation.accountToOrderHistory

      - executeFunction:
          name: us.functions.orderHistory.serachBar.SearchYourPurchasesTextbox

     # Search and view details of order from purchase history page
      - executeFunction:
          name: us.functions.orderHistory.searchOrder
          params:
              - name: orderNumber
                string: ${orderNo}

        # Click on start a return
      - executeFunction:
          name: us.functions.orderHistory.startAReturn

      # - sleep:
      #     duration: 10000
  
      # # Deeplink into order details page
      # - executeFunction:
      #     name: us.functions.global.navigation.goToDeepLink
      #     params:
      #       - name: deepLinkUrl
      #         string: walmart://orders/$orderNo

      # # Initiate returns
      # - executeFunction:
      #     name: us.functions.returns.goToStartReturn
      
      # Verify return screen display
      - executeFunction:
          name: us.functions.returns.verifyStartReturnScreen

      # Item selection for replacement
      - executeFunction:
          name: us.functions.returns.itemReturn
          params:
              - name: itemIndex
                string: 1 
              - name: returnReason
                string: Item arrived damaged
              - name: returnType
                string: Replacement
              - name: replacementType
                string: fast
              - name: returnMethod
                string: Return by mail
              - name: itemType
                string: fc
              - name: returnMode
                string: MAIL
              - name: itemId
                string: $fcItem
              - name: orderNo
                string: $orderNo

      - sleep:
          duration : 60000

      # - executeFunction:
      #     name: us.functions.global.navigation.goToDeepLink
      #     params:
      #       - name: deepLinkUrl
      #         string: walmart://orders/$orderNo

      # # Validate replacement confirmation in ODP
      # - executeFunction:
      #     name: us.functions.returns.returnODPValidation

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    flow:
      - executeFunction:
          name: us.functions.utils.afterSteps
