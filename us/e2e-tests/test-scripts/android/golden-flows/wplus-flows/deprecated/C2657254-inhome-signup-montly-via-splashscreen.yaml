general:
  platform: android
  tags: C2657254 #(Deprecated as part of Regression Optimization) us-golden-flows-wplus, C2657254, p1-wplus-teflon-e2e
  testCaseId: C2657254
  inherit:
    filesRunAll:
      - us-errors-helpers.yaml
  metadata:
    - name: scenarioId
      string: 288
scenarios:
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
  # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
  - name: Main
    flow:
      - log: Start Main

      # Get timestamp
      - getTimestamp:
          asDate: false
          storeIn: timestamp
      - storeIn:
          key: customerEmail
          value: wmtuser${timestamp}@walmart.com

      # Create Accountusing astro api
      - executeFunction:
          name: us.functions.utils.astro.createTrialAccountConvertToPaid
          params:
            - name: email
              string: $customerEmail
            - name: payload
              string: us.data.flowinhome.createAccountPayload.monthly

      # Navigate onboarding to home
      - executeFunction:
          name: us.functions.global.onboarding.navigateOnboardingToHome

      - executeFunction:
          name: us.functions.utils.handleTechnicalErrorPopup

      # Navigate to sign in page
      - executeFunction:
          name: us.functions.home.navigateHomeToSignIn

      # Sign in with default account
      - executeFunction:
          name: us.functions.global.authentication.signInAccount
          params:
            - name: email
              string: $customerEmail
            - name: password
              string: E2epassword$

      # navigate to Home Screen
      - executeFunction:
          name: us.functions.global.navigation.accountToHome

      # Select inhome delivery address
      - executeFunction:
          name: us.functions.gic.select.inhome.delivery.address

      # Terminate and Relaunch the app
      - executeFunction:
          name: us.test.functions.utils.relaunchApp

      # Verify inhome splashscreen is displayed
      - executeFunction:
          name: us.funtions.inHome.splashscreen.freetrial.signup

      # Navigate to Accounts from tab bar
      - executeFunction:
          name: us.functions.global.navigation.goToAccountFromTabBar

      # Navigate to Wplus
      - executeFunction:
          name: us.functions.wplus.account.navigateToWalmartPlus #us.functions.wplus.navigateToWalmartPlus

      # Navigate to manage membership screen
      - executeFunction:
          name: us.functions.wplus.navigateToManageMembershipFromBenefitsPage

      # Verify inHome plusUp details on Manage Membership flow
      - executeFunction:
          name: us.functions.wplus.inHome.validateMyPlusUpsDetailsOnManageMembershipPage
          params:
            - name: tenure
              string: monthly
            - name: membership
              string: paid

      - storeIn:
          key: testStatus
          value: passed
  - name: After
    after: true
    useTestConfigErrors: false
    flow:
      - executeFunction:
          name: us.functions.utils.afterSteps
