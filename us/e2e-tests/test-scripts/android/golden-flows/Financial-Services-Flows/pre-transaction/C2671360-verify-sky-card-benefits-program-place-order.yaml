# Non-amendable
general:
  platform: android
  tags: C2671360, p2-ecomm-teflon-e2e
  inherit:
    filesRunAll:
      - us-errors-helpers.yaml
  testCaseId: C2671360
  metadata:    
   - name: scenarioId      
     string: FS008	


scenarios:
# Android - Verify-sky-card-benefits-program-place order
  - name: Before
    before: true
    endTestOnFailure: true
    flow:

  - name: Main
    endTestOnFailure: true
    flow:

      # - executeFunction:
      #     name: us.functions.utils.addAmountToDsSkyCard
      #     params:
      #       - name: balance
      #         string: 30
      #       - name: PIHASH
      #         string: PIH.pang.WMTPC.GIFTCARD.0e9ed288-83db-4795-80cd-bee75cf0ea4d.8334

      - log: Start Main

      - executeFunction:
          name: us.functions.utils.astro.clearCart
          params:
            - name: custEmail
              string: us.data.android.flowSkyCard1.email #us.data.android.flowSkyCard.email

      #Navigate onboarding to home
      - executeFunction:
          name: us.functions.global.onboarding.navigateOnboardingToHome

      # Will take the user to the account menu from where the user can sign in to the app
      - executeFunction:
          name: us.functions.home.navigateHomeToSignIn

      # User signs in to the account
      - executeFunction:
          name: us.functions.global.authentication.signInAccount
          params:
            - name: email
              string: us.data.android.flowSkyCard1.email #us.data.android.flowSkyCard.email
            - name: password
              string: us.data.android.flowSkyCard.password

      - executeFunction:
          name: us.functions.global.navigation.accountToHome

      - executeFunction:
          name: us.functions.home.changeStore
          params:
              - name: zipcode
                string: 95122
              - name: storeName
                string: San Jose Supercenter

      - executeFunction:
          name: us.functions.gic.changeFulfillment
          params:
            - name: option
              string: Shipping

      - executeFunction:
           name: us.functions.utils.fetchFromDataList 
           params:
             - name: email
               string: us.data.android.flowSkyCard1.email
             - name: itemType
               string: FC_DELIVERY
             - name: itemList
               string: us.data.flow.dsSkyCard.fcItemLists

     # searching for OTC or Food item
      - executeFunction:
          name: us.functions.search.searchItem
          params:
            - name: searchTerm
              string: ${itemFetched}

      - executeFunction:
          name: us.functions.search.clickOnFirstItem

      # - executeFunction:
      #     name: us.functions.services.benefitCenter.clickOnCarouselItemOnBenefitCenterProgramPage

      - executeFunction:
          name: us.functions.item.benefitCenter.verifyBCBSEligibility

      - executeFunction:
          name: us.functions.item.benefitCenter.clickOnDetailsBtn

      - executeFunction:
          name: us.functions.item.benefitCenter.verifyBCBSDtlsCTA

      - executeFunction:
          name: us.functions.item.addItemToCart

      - executeFunction:
          name: us.functions.global.navigation.goToServicesFromTabBar
      
      - executeFunction:
          name: us.functions.services.verificationBenefitPrograms
      
      - executeFunction:
          name: us.functions.services.goToBCBSA

      - executeFunction:
          name: us.functions.services.benefitCenter.validateBenefitCenterProgramPage
      
      - executeFunction:
          name: us.functions.services.benefitCenter.clickOnViewBnftTrackr

      - executeFunction:
          name: us.functions.services.benefitCenter.verifyBnftTrackrCTA

      # Go to cart
      - executeFunction:
           name: us.functions.global.navigation.toCartFromNavBar

      - executeFunction:
          name: us.functions.cart.switchBetweenPickUpAndShipping
          params:
            - name: fulfillmentType
              string: shipping

      # - executeFunction:
      #     name: us.functions.selectBookingSlot
      #     params:
      #       - name: tab
      #         string: pickup
      #       - name: page
      #         string: cart
      #       - name: timeSlot
      #         string: unscheduled
 
      - executeFunction:
          name: us.functions.cart.verifyBnftsTrackr

      - executeFunction:
          name: us.functions.services.benefitCenter.clickOnViewBnftTrackr

      - executeFunction:
          name: us.functions.services.benefitCenter.verifyBnftTrackrCTA

    # Continue to checkout
      - executeFunction:
          name: us.functions.cart.continueToCheckout

      - executeFunction:
          name: us.functions.reviewOrder.verifySkyBenefitCard

      - executeFunction:
          name: us.functions.checkout.placeOrder

      - executeFunction:
          name: us.functions.orderConfirmation.getOrderNumber

      - log: ${orderNo}

      # Deeplink into order details page
      - executeFunction:
          name: us.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: walmart://orders/$orderNo

      - executeFunction:
          name: us.functions.oderDetailPage.verifySkyBenefitCard
            
      # - executeFunction:
      #     name: us.functions.utils.astro.getOrderStatus
      #     params:
      #       - name: orderNo
      #         string: ${orderNo}
      # - executeFunction:
      #     name: us.functions.utils.astro.setOrderStatus
      #     params:
      #       - name: orderNo
      #         string: ${orderNo}
      #       - name: orderType
      #         string: STORE_UNSCHEDULED_PICKUP
      #       - name: status
      #         string: PO Ready For Pickup

      # Verify order status in order details page
      # - executeFunction:
      #     name: us.functions.orderDetails.ost.pickup.verifyOrderStatus
      #     params:
      #       - name: fulfillmentType
      #         string: pickup
      #       - name: orderStatus
      #         string: Ready

      # - executeFunction:
      #     name: us.functions.utils.astro.getOrderStatus
      #     params:
      #       - name: orderNo
      #         string: ${orderNo}
          
      # - executeFunction:
      #     name: us.functions.utils.astro.setOrderStatus
      #     params:
      #       - name: orderNo
      #         string: ${orderNo}
      #       - name: orderType
      #         string: STORE_UNSCHEDULED_PICKUP
      #       - name: status
      #         string: Customer Picked

      # # Deeplink into order details page
      # - executeFunction:
      #     name: us.functions.global.navigation.goToDeepLink
      #     params:
      #       - name: deepLinkUrl
      #         string: walmart://orders/$orderNo          

      # # go to order details
      # - executeFunction:
      #     name: us.functions.orderDetails.ost.pickup.verifyOrderStatus
      #     params:
      #       - name: fulfillmentType
      #         string: pickup
      #       - name: orderStatus
      #         string: Picked up

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    flow:
      - executeFunction:
          name: us.functions.utils.afterSteps

