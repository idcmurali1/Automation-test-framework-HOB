# Amendable
general:
  platform: ios
  tags: C2644798, us-golden-flows, P2, p2-ecomm-teflon-e2e, EC012
  inherit:
    filesRunAll:
      - us-errors-helpers.yaml
  testCaseId: C2644798
  metadata:
    - name: scenarioId
      string: EC012 #16

scenarios:
  - name: Main
    testCaseId: C2126544
    flow:
      # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
      # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      - log: Start Main

      #Navigate onboarding to home
      - executeFunction:
          name: us.functions.global.onboarding.navigateOnboardingToHome
      # Will take the user to the account menu from where the user can sign in to the app
      - executeFunction:
          name: us.functions.home.navigateHomeToSignIn

      # User signs in to the account
      - executeFunction:
          name: us.functions.global.authentication.signInAccount
          params:
            - name: email
              string: us.data.flow012a.email
            - name: password
              string: us.data.flow012a.password

      # - executeFunction:
      #     name: us.functions.utils.getSearchableItem
      #     params:
      #       - name: source
      #         string: astro
      #       - name: itemType
      #         string: STORE_SCHEDULED_DELIVERY
      #       - name: storeId
      #         string: 32144
      #       - name: backupItem
      #         string: 967762968

      - executeFunction:
          name: us.functions.utils.fetchItemAndValidate
          params:
            - name: itemType
              string: STORE_SCHEDULED_DELIVERY
            - name: storeId
              string: 32144
            - name: action
              string: FetchItem
            - name: emailId
              string: us.data.flow012a.email


      - storeIn:
          key: item1
          value: $itemFetched

      - log: here is the item 1 fetched  ${item1}

      # - executeFunction:
      #     name: us.functions.utils.getSearchableItem
      #     params:
      #       - name: source
      #         string: astro
      #       - name: itemType
      #         string: MP_DELIVERY
      #       - name: store
      #         string: 32144
      #       - name: backupItem
      #         string: 694602569

      - executeFunction:
          name: us.functions.utils.fetchItemAndValidate
          params:
            - name: itemType
              string: MP_DELIVERY
            - name: storeId
              string: 32144
            - name: action
              string: FetchItem
            - name: emailId
              string: us.data.flow012a.email
      - storeIn:
          key: item2
          value: $itemFetched

      # Astro - Create order
      - executeFunction:
          name: us.functions.utils.astro.createOrder
          params:
            - name: payload
              string: us.data.flow012a.amendablePayload

      # Deeplink into order details page
      - executeFunction:
          name: us.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: walmart://orders/$orderNo

      - executeFunction:
          name: us.functions.cancel.orderItems
          params:
            - name: itemTypes
              string: sc,mp
            - name: reasonIndex
              string: 3

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    useTestConfigErrors: false
    flow:
      - executeFunction:
          name: us.functions.utils.afterSteps
