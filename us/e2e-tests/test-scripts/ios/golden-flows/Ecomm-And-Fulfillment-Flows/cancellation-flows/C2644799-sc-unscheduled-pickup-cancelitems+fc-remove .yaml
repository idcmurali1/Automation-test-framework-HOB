general:
  platform: ios
  tags: us-golden-flows, us-golden-flows-ecom-p1, C2644799, p2-ecomm-teflon-e2e
  inherit:
    filesRunAll:
      - us-errors-helpers.yaml
  testCaseId: C2644799
  metadata:
    - name: scenarioId
      string: EC013

scenarios:

  - name: Before
    before: true
    endTestOnFailure: true
    flow:

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

#*******************************************************************************************************

      # Astro - Create order and move to PO Acknowledged

#*******************************************************************************************************

      - storeIn:
          key: skipNextGen
          value: true

      # # Fetch stock available items using fetch API
      # - executeFunction:
      #     name: us.functions.utils.getSearchableItem
      #     params:
      #       - name: source
      #         string: astro
      #       - name: itemType
      #         string: STORE_UNSCHEDULED_PICKUP
      #       - name: store
      #         string: 32144
      #       - name: backupItem
      #         string: 499312381

      - executeFunction:
          name: us.functions.utils.fetchItemAndValidate
          params:
            - name: itemType
              string: STORE_UNSCHEDULED_PICKUP
            - name: storeId
              string: 32144
            - name: action
              string: FetchItem
            - name: emailId
              string: us.data.flow013.email
            
      - storeIn:
          key: item1
          value: $itemFetched

      # Fetch stock available items using fetch API
      # - executeFunction:
      #     name: us.functions.utils.getSearchableItem
      #     params:
      #       - name: source
      #         string: astro
      #       - name: itemType
      #         string: FC_DELIVERY
      #       - name: store
      #         string: 32144
      #       - name: backupItem
      #         string: 945213102

      - executeFunction:
          name: us.functions.utils.fetchItemAndValidate
          params:
            - name: itemType
              string: FC_DELIVERY
            - name: storeId
              string: 32144
            - name: action
              string: FetchItem
            - name: emailId
              string: us.data.flow013.email
      - storeIn:
          key: item2
          value: $itemFetched   
      
      - executeFunction:          
          name: us.functions.utils.astro.createOrder
          params:
            - name: payload
              string: us.data.flow013.nonAmendablePayload

  - name: Main
    testCaseId: C2096058
    flow:

      - log: Start Main

      # # Navigate onboarding to home
      - executeFunction:
          name: us.functions.global.onboarding.navigateOnboardingToHome

      # Will take the user to the account menu from where the user can sign in to the app
      - executeFunction:
          name: us.functions.home.navigateHomeToSignIn

      # User signs in to the account
      - executeFunction:
          name: us.functions.global.authentication.signInAccount
          params:
            - name: email
              string: us.data.flow013.email
            - name: password
              string: E2epassword$

      # Deeplink into order details page
      - executeFunction:
          name: us.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: walmart://orders/$orderNo

      # Cancel items in order either with instant or request cancellation methods and validate
      - executeFunction:
          name: us.functions.cancel.orderItems
          params:
            - name: itemTypes
              string: sc,fc

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    useTestConfigErrors: false
    flow:
      - executeFunction:
          name: us.functions.utils.afterSteps
