general:
  platform: ios
  tags: helpcenter
  # inherit:
  #   filesRunAll:
  #     - errors-helpers.yaml

scenarios:
  #--------------------------------------------------------------------------------------------------------------------
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: TEST START - Help Regression - 001 - Before Flow
      # - executeFunction:
      #     name: mx.functions.welcome.assertPageDisplayed

  #--------------------------------------------------------------------------------------------------------------------
  - name: Validate the helpcenter navigation
    flow:
      - log: TEST START - Help Regression - 001

      # Navigate Onboarding Screens to Home Screen...
      - executeFunction:
          name: mx.flows.global.onboarding.navigateToHomeAsGuestUser
          params:
            - name: zipCode
              string: mx.data.e2e-regression.od-002.zipCode
            - name: banner
              string: "OD"

      - executeFunction:
          name: mx.functions.helpCentre.navigateToHelpCentre