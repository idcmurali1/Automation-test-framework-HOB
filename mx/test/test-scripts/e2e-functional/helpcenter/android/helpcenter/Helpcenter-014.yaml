general:
  platform: android
  tags: helpcenter

scenarios:

  #-------------------------------------------------------------------------------------------------------------------- 
  - name: Validate the whatsApp in the search page
    flow:
      - log: "TEST START - Help Regression - 009"

      - executeFunction:
          name: mx.flows.global.onboarding.navigateToHomeAsGuestUser
          params:
            - name: zipCode
              string: mx.data.e2e-regression.od-002.zipCode
            - name: banner
              string: "OD"

      - executeFunction:
          name: mx.functions.helpCentre.navigateToHelpCentre

      - executeFunction:
          name: mx.functions.helpCentre.verifyHelpCentreScreenElements

      - executeFunction:
         name: mx.functions.helpCentre.enterSearch
         params:
           - name: helpsearch
             string: "pedidos"

      - executeFunction:
         name: mx.functions.helpCentre.clickOnSearchFor

      - executeFunction:
         name: mx.functions.helpCentre.verifyWhatsAppButtonPresent