general:
  platform: android
  tags: helpcenter

scenarios:

  #-------------------------------------------------------------------------------------------------------------------- 
  - name: Validate no search results page with search again
    flow:
      - log: "TEST START - Help Regression - 011"

      - executeFunction:
          name: mx.flows.global.onboarding.navigateToHomeAsGuestUser
          params:
            - name: zipCode
              string: mx.data.e2e-regression.od-002.zipCode
            - name: banner
              string: "OD"
      
      - executeFunction:
          name: mx.functions.helpCentre.navigateToHelpCentre

      - executeFunction:
          name: mx.functions.helpCentre.verifyHelpCentreScreenElements

      - executeFunction:
         name: mx.functions.helpCentre.enterSearch
         params:
           - name: helpsearch
             string: "jdfhjfhdjfdj"

      - executeFunction:
          name: mx.functions.helpCentre.clickOnSearchFor

      - executeFunction:
          name: mx.functions.helpCentre.clickSearchAgain

      - executeFunction:
          name: mx.functions.helpCentre.verifySearchInput