general:
  platform: android
  tags: helpcenter

scenarios:

  #-------------------------------------------------------------------------------------------------------------------- 
  - name: Validate article feedback with no and select valid option
    flow:
      - log: "TEST START - Help Regression - 007"

      - executeFunction:
          name: mx.flows.global.onboarding.navigateToHomeAsGuestUser
          params:
            - name: zipCode
              string: mx.data.e2e-regression.od-002.zipCode
            - name: banner
              string: "OD"

      - executeFunction:
          name: mx.functions.helpCentre.navigateToHelpCentre

      - executeFunction:
          name: mx.functions.helpCentre.verifyHelpCentreScreenElements

      - executeFunction:
         name: mx.functions.helpCentre.clickChannel

      - executeFunction:
         name: mx.functions.helpCentre.clickArticle

      - executeFunction:
         name: mx.functions.helpCentre.clickOnValidOptionAndSubmit