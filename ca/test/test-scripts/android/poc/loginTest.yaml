general:
  platform: android
  tags: ca_authpoc1
  testCaseId: 000001


scenarios:
  - name: Verify Sign in functionality
    flow:
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
      - executeFunction:
          name: ca.functions.home.navigateHomeToSignIn
    #Signin  
      - executeFunction:
          name: ca.functions.global.authentication.signInAccount
          params:
              - name: email
                string: ca.data.test.email
              - name: password
                string: ca.data.test.password
      - verifyIdentifier:
          timeout: 30000
          present:
            - identifier: ca.mappings.account.yourOrdersButton

      - log: Tests Completed successfully