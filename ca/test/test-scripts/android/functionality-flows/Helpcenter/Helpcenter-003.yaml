general:
  platform: android
  tags: helpcenter

scenarios:

  #--------------------------------------------------------------------------------------------------------------------
  - name: Validate the article feedback
    flow:
      - log: "TEST START - Help Regression - 003"

      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateOnboardingToHome.paramZip
          params:
            - name: ZIP_CODE
              string: ca.test.data.onboarding.zipcode
              
      - executeFunction:
          name: ca.test.functions.helpCentre.navigateToHelpCentre

      - executeFunction:
          name: ca.test.functions.helpCentre.verifyHelpCentreScreenElements

      - executeFunction:
         name: ca.test.functions.helpCentre.clickChannel

      - executeFunction:
         name: ca.test.functions.helpCentre.clickArticle

      - executeFunction:
         name: ca.test.functions.helpCentre.articleYes