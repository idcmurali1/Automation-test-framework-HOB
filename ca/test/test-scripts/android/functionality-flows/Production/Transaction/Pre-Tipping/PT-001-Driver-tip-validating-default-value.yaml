general:
  platform: Android
  tags: PT-001, ca-function-run
  inherit:
    filesRunAll:
      - ca-errors-helpers.yaml

scenarios:
  - name: Before
    before: false
    endTestOnFailure: true
    flow:
      - log: Execute before scenarios

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
#* * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
  - name: Driver tip validating default value
    flow:
      - log:
         message: Driver tip validating default value
         color: PURPLE_BOLD_BRIGHT

      - log: 
          message: Onboarding to app using postal code.
          color: GREEN_BOLD  

      - executeFunction:
          name: ca.test.functions.onboardingToHomePage.usingPostalCode
          params:
            - name: PostalCode
              string: ca.test.data.orleansZipCode   
      
      - executeFunction:
          name: ca.test.functions.global.navigation.goToAccountFromTabBar

      # Creating Account from Account
      - executeFunction:
          name: ca.test.functions.loginPage.createAccountPage.createNewAccount
          params:
            - name: firstName
              string: ${firstName}
            - name: lastName
              string: ${lastName}
            - name: email
              string: ${email}
            - name: password
              string: ${password}

      # Account page navigating to address page
      - executeFunction:
          name: ca.test.functions.accountPage.goToAddresses
      
      # Clicking on add new address
      - executeFunction:
          name: ca.test.functions.accountPage.addNewAddressButton
      
      # Entering new address details
      - executeFunction:
          name: ca.test.functions.accountPage.addresses.addNewAddress
          params:
            - name: fName
              string: ca.test.data.global.firstName
            - name: lName
              string: ca.test.data.global.lastName
            - name: streetAddress
              string: ca.test.data.PT.addressFirstLine
            - name: city
              string: ca.test.data.PT.addressCity
            - name: state
              string: ca.test.data.PT.addressState
            - name: zipcode
              string: ca.test.data.PT.deliveryAddresszipCode
            - name: mobileNumber
              string: ca.test.data.global.mobileNumber         

      - executeFunction:
         name: ca.test.functions.global.navigateToShop  
   
      - log: 
          message: Selecting store at HomePage.
          color: GREEN_BOLD

      - executeFunction:
          name: ca.test.functions.home.selectStore
          params:
            - name: Zip_Code
              string: ca.test.data.orleansZipCode
            - name: storeName
              string: ca.test.data.orleansStoreName

      - executeFunction:
          name: ca.test.functions.home.changeLocationLink
          params:
            - name: pinCode
              string: ca.test.data.PS-001.selectAddressZipCode        

      - log: 
          message: Searching for a GO product with product name.
          color: GREEN_BOLD
      
      - executeFunction:
          name: ca.functions.searchPage.searchItemArray
          params:
            - name: items
              string: ca.test.data.PT-001.goItemArray1
            - name: quantity
              string: ca.test.data.PS002.product_1_qty 
            - name: checkNonPromotionBadges
              string: true

      - executeFunction:
          name: ca.functions.searchPage.searchItemArray
          params:
            - name: items
              string: ca.test.data.PT-001.goItemArray2
            - name: quantity
              string: ca.test.data.product_1_qty
            - name: checkNonPromotionBadges
              string: true

      - executeFunction:
          name: ca.functions.searchPage.searchItemArray
          params:
            - name: items
              string: ca.test.data.PT-001.goItemArray3
            - name: quantity
              string: ca.test.data.product_1_qty
            - name: checkNonPromotionBadges
              string: true     

      - executeFunction:
          name: ca.test.functions.cart.assertCartTotal
          params:
            - name: subTotal
              string: ${totalPrice}

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      - log:
          message: Navigating to cart.
          color: GREEN_BOLD

      - executeFunction:
          name: ca.test.functions.global.navigation.goToCart

      - log:
          message: Expanding the product tile if compressed.
          color: GREEN_BOLD

      - executeFunction:
          name: ca.test.functions.cart.productCardExpansion

      # Clicking on reserve time button
      - executeFunction:
          name: ca.test.functions.cart.reserveTimePage

      # Validating reserve time page
      - executeFunction:
          name: ca.test.functions.reserveTimePageValidation

      # clicking navigate to deliver from store page
      - executeFunction:
          name: ca.test.functions.reserveTime.deliverFromStore

      # Reserving a slot
      - executeFunction:
          name: ca.test.functions.bookASlot.reserveTimePage.reserveStandardSlot

      # Clicking on book a slot button
      - executeFunction:
          name: ca.test.functions.bookASlot.reserveTimePage.bookASlotButton    

      # Proceed to Checkout
      - executeFunction:
          name: ca.test.functions.cart.proceedToCheckOut

      - executeFunction:
         name: ca.test.functions.checkOut.driverTipCalculation
         params:
           - name: subTotal
             string: ${totalPrice}

      - executeFunction:
          name: ca.test.functions.checkout.driverTip.iBtnValidation

  #After Scenarios
  - name: After
    after: true
    flow:
      - executeFunction:
          name: ca.test.functions.utils.afterSteps     

          
