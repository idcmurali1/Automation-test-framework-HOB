general:
  platform: android
  tags: ca-example-test
  # testCaseId: C000001
  # inherit:
  #   filesRunAll:
  #     - default-helpers.yaml

scenarios:

  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: Execute before steps

  - name: Main
    flow:
      - log: Execute main steps

  - name: After
    after: true
    flow:
      - log: Execute after steps
