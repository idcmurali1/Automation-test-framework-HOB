  # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
# scenarios covered:
#Onboarding, Postal Code Entry, Astro - Clear Cart
# signin, Selecting store in home page
# add product - deeplink
#Click See Times, bok a delivery slot
#Clicking the cart, Proceed to Checkout, add cvv
# place order and order cancellation
  # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

general:
  platform: ios
  tags: flow-045, ca_authpoc, fr_ca, E2E-iOS-English, E2E-iOS-French
  inherit:
    filesRunAll:
      - ca-errors-helpers.yaml

scenarios:
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: Execute before steps

      # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
  # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

  - name: Main
    flow:

      - log: Start Main

      #Get TimeStamp
      - getTimestamp:
          asDate: false
          storeIn: timestamp
          
      #temporary#
      - executeFunction:
          name: ca.test.functions.utils.avoidTestPopup

      # Navigate onboarding to home
      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateiosOnboardingToHome
          params:
            - name: ZIP_CODE
              string: ca.test.data.store.zipCode

      #Verifying home screen
      - executeFunction:
          name: ca.test.functions.home.headerElements.validateIosHomePage

      - executeFunction:
          name: ca.test.functions.home.livingDesign.validateIosBottomNavigation

     # Astro - Clear Cart
      - executeFunction:
         name: ca.functions.utils.astro.clearCart
         params:
           - name: custEmail
             string: ca.test.data.flow045.emailios
           - name: password
             string: ca.test.data.flow045.passwordios

      # Navigate to sign in page
      - executeFunction:
          name: ca.test.functions.home.signIn

      - executeFunction:
          name: ca.test.functions.loginPage.userLogin
          params:
            - name: email
              string: ca.test.data.flow045.emailios
            - name: password
              string: ca.test.data.flow045.passwordios

      - executeFunction:
          name: ca.test.functions.utils.terminateApp

      - executeFunction:
          name: ca.test.functions.utils.relaunchApp

       #Selecting store in home page
      - executeFunction:
          name: ca.test.functions.home.storeSelect
          params:
            - name: Zip_Code
              string: ca.test.data.store.zipCodeOrleans
            - name: storeName
              string: ca.test.data.store.storeNameOrleans

      # Product Search
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:            
            - name: product
              string: ca.test.data.flow045.product_P1_sku

      # Add to cart
      - executeFunction:
          name: ca.test.functions.productSearch.addToCart
          params:
            - name: productName
              string: ca.test.data.flow045.product_P1_Name
            - name: quantity
              string: ca.test.data.flow045.product_P1_Quantity

      #clear search box
      - executeFunction:
          name: ca.test.functions.productSearch.clearSearch

      # Product Search
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:            
            - name: product
              string: ca.test.data.flow045.product_P2_sku

      # Add to cart
      - executeFunction:
          name: ca.test.functions.productSearch.addToCart
          params:
            - name: productName
              string: ca.test.data.flow045.product_P2_Name
            - name: quantity
              string: ca.test.data.flow045.product_P2_Quantity

      # Validate cart quantity
      - executeFunction:
           name: ca.test.functions.cart.assertCartQuantity
           params:
            - name: cartQuantity
              string: ca.test.data.flow045.cartQuantity   

      # Asserting cart total
      - executeFunction:
          name: ca.test.functions.cart.assertCartTotal
          params:
            - name: subTotal
              string: ca.test.data.flow045.cartSubTotal

      - executeFunction:
          name: ca.test.functions.global.navigation.goToShopTab

      # Click See Times
      - executeFunction:
          name: ca.test.function.seeTimes.click

      # Book pickup standard slot
      - executeFunction:
          name: ca.test.functions.home.seeTimes.bookStandardSlot

# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      #Clicking the cart
      - executeFunction:
          name: ca.test.functions.cart.clickTheCartIcon
      
      - if:
          identifier:
            present:
              - identifier: ca.test.mappings.cart.amendOrder.existingOrder.amendTime
         
          then:

            - executeFunction:
                name: ca.test.functions.cart.amendOrder.existingOrder.addItems

          else:

              # Cart fulfillment verification
              - executeFunction:
                  name: ca.test.functions.cart.scheduledPickup.verifyCard.ios

              - executeFunction:
                  name: ca.test.functions.cart.shippingwithPickup.verifyCard

              # Cart product details validation
              - executeFunction:
                  name: ca.test.functions.cart.verifyProductTitle
                  params:
                    - name: productName
                      string: ca.test.data.search.flow.045.subscriptionItem.itemName

              # Cart product details validation
              - executeFunction:
                  name: ca.test.functions.cart.verifyProductTitle
                  params:
                    - name: productName
                      string: ca.test.data.search.flow.045.Go.itemName

              # Verify cart subTotal
              - executeFunction:
                  name: ca.test.functions.cart.cartSubTotal
                  params:
                    - name: subTotal
                      string: ca.test.data.flow045.subtotal

              # # Removing $ from slot price
              # - executeNode:
              #     file: ca/test/helpers/sanitizePriceIos.js
              #     args:
              #       - value: ${slotPrice}
              #     getResponse:
              #       storeIn: clearPrice
              # - log: ${clearPrice}      

     # Temporary Commenting Associate discout validation
              # Verify cart associate discount
              # - executeFunction:
              #     name: ca.test.functions.cart.associateDiscountAssertion
              #     params:
              #       - name: associateDiscount
              #         string: ca.test.data.assoDiscount.flow045

              # # Asserting Grocery delivery charges in cart page 
              # - executeFunction:
              #     name: ca.test.functions.cart.groceryPickup
              #     params:
              #       - name: groceryFee
              #         string: ${clearPrice} 

              # Verify cart walmart shipping
              - executeFunction:
                  name: ca.test.functions.cart.cartWalmartShipping
                  params:
                    - name: walmartShipping
                      string: ca.test.data.flow045.walmartShipping

              # # Tax calculation
              # - executeFunction:
              #     name: ca.test.functions.cart.taxCalculation
              #     params:
              #       - name: subTotal
              #         string: ca.test.data.flow045.subtotal_ios
              #       - name: slotPrice
              #         string: ${clearPrice}
              #       - name: tax
              #         string: ca.test.data.search.hstPercentage         

              # # Verify cart Taxes
              # - executeFunction:
              #     name: ca.test.functions.cart.cartTaxes
              #     params:
              #       - name: taxes
              #         string: ${tax_values} 

              # # Verify cart HST
              # - executeFunction:
              #     name: ca.test.functions.cart.hstTaxAssertion
              #     params:
              #       - name: hst_percentage
              #         string: ca.test.data.search.hstPercentage
              #       - name: hst_tax_value
              #         string: ${tax_values} 

              # - executeFunction:
              #     name: ca.test.functions.cart.estimatedTotalCalculation
              #     params:
              #       - name: tax
              #         string: ${tax_values}
              #       - name: subTotal
              #         string: ca.test.data.flow045.subtotal_ios
              #       - name: slotPrice
              #         string: ${clearPrice}         

              # # Verify cart estimated total
              # - executeFunction:
              #     name: ca.test.functions.cart.cartEstimatedTotal
              #     params:
              #       - name: estimate_total
              #         string: ${estimate_total}

              # Proceed to Checkout
              - executeFunction:
                  name: ca.test.functions.cart.proceedToCheckOutIos

              # CheckOut page order review validation
              - executeFunction:
                  name: ca.test.functions.checkOut.validateCheckOutOrderCard
                  params:
                    - name: fulfillmentType
                      string: In-store pickup

              # # Validating payment card
              # - executeFunction:
              #     name: ca.test.functions.checkOut.paymentOptionsIos 

              # Add Card Cvv
              - executeFunction:
                  name: ca.test.functions.checkOut.payment.addCvvIos
                  params:
                    - name: CVV
                      string: ca.test.data.checkout.Cvv_1

              # Adding mobile number
              - executeFunction:
                  name: ca.test.functions.checkout.phoneNumber.enterContactNumber        

              # Checkout page sub total assertion
              - executeFunction:
                  name: ca.test.functions.checkOut.subTotalValidation
                  params:
                    - name: subTotal
                      string: ca.test.data.flow045.subtotal

      # Temporary Commenting Associate discount validation
              # Verify Checkout page associate discount
              # - executeFunction:
              #     name: ca.test.functions.checkOut.associateDiscountAssertion
              #     params:
              #       - name: associateDiscount
              #         string: ca.test.data.assoDiscount.flow045

              # # Verify checckout pickup Fee --$1 and FREE issue
              # - executeFunction:
              #     name: ca.test.functions.checkOut.groceryPickup
              #     params:
              #       - name: groceryFee
              #         string: ${clearPrice} 

              # Verify cart walmart shipping
              - executeFunction:
                  name: ca.test.functions.checkOut.walmartShippingAssertion
                  params:
                    - name: walmartShipping
                      string: ca.test.data.flow045.walmartShipping

              # # Checkout page tax assertion
              # - executeFunction:
              #     name: ca.test.functions.checkOut.taxesAssertion
              #     params:
              #       - name: total_tax
              #         string: ${tax_values} 

              # # Checkout page hst assertion
              # - executeFunction:
              #     name: ca.test.functions.checkOut.hstAssertion
              #     params:
              #       - name: hst_percentage
              #         string: ca.test.data.search.hstPercentage
              #       - name: hst_tax_value
              #         string: ${tax_values}  

              # # Checkout page estimate total assertion
              # - executeFunction:
              #     name: ca.test.functions.checkOut.estimateTotalAssertion
              #     params:
              #       - name: estimate_total
              #         string: ${estimate_total}
              
              # Checkout
              - executeFunction:
                  name: ca.test.functions.checkOut.PlacingOrder

        # # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
              - log: 'R2_SUBFLOW_POST_TRANSACTION'
        # # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *          

              # Order Confirmation screen validation
              - executeFunction:
                  name: ca.test.functions.checkOut.orderConfirmationValidationIos

              # # Cancelling Order
              # - executeFunction:
              #     name: ca.test.functions.purchaseHistory.orderCancellation

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    flow:
      - executeFunction:
          name: ca.test.functions.utils.afterSteps