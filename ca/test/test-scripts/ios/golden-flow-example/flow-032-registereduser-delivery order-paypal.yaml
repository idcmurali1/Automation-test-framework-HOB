#-------------------------------------------------------------------------------------------------------------------
# scenarios covered:
# Navigate onboarding to home and clear cart through astro
# Checking and selecting Store in HP
# Navigate to sign in page and signin
# search a product through deep link and A2C from pdp
# navigate to cart and book a delivery slot from cart
# verify fulfillment and product details in cart
# checkout and validate payment card fulfilemt type in checkout page
# Paypal Payment 
# Validate mobile number in checkout page
# place the order and validate order confirmation page
#order cancellation through account page
#-------------------------------------------------------------------------------------------------------------------
general:
  platform: ios
  tags: flow-032, ca_authpoc, testrun1, fr_ca, group-4, E2E-iOS-English, E2E-iOS-French
  inherit:
    filesRunAll:
      - ca-errors-helpers.yaml

scenarios:
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: Execute before steps

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

  - name: Main
    flow:

      - log: Start Main

      # Get TimeStamp
      - getTimestamp:
          asDate: false
          storeIn: timestamp  
          
      #temporary#
      - executeFunction:
          name: ca.test.functions.utils.avoidTestPopup

      # Navigate onboarding to home
      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateiosOnboardingToHomeparamZip
          params:
            - name: ZIP_CODE
              string: ca.test.data.flow032.zipCode

      # Creating account using astro
      - executeFunction:
          name: ca.test.functions.astro.createAccount
          params:
            - name: payLoad
              string: ca.test.data.flow032.astroRequestBody

      # Home to Sign In
      - executeFunction:
          name: ca.test.functions.home.signIn

      # Sign In to the account
      - executeFunction:
          name: ca.test.functions.loginPage.userLogin
          params:
            - name: email
              string: ca.test.data.flow032.email
            - name: password
              string: ca.test.data.flow032.password

      - executeFunction:
          name: ca.test.functions.utils.terminateApp

      - executeFunction:
          name: ca.test.functions.utils.relaunchApp

      # Selecting store from home page
      - executeFunction:
          name: ca.test.functions.home.storeSelect
          params:
            - name: Zip_Code
              string: ca.test.data.flow032.zipCode
            - name: storeName
              string: ca.test.data.flow032.storeAddress

       # Selecting address at home page
      - executeFunction:
          name: ca.test.functions.home.changeLocationLink
          params:
            - name: address
              string: ca.test.data.flow032.zipCodeAddress

      # Product Search using sku
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:           
            - name: product
              string: ca.test.data.flow032.product_P1_sku

      # # Product Details Validation
      # - executeFunction:
      #     name: ca.test.functions.pdp.productDetailsValidationIos
      #     params:
      #       - name: productType
      #         string: GO

      # Navigating to PDP from SRP
      - executeFunction:
          name: ca.test.functions.productSearch.srpToPdpPage
          params:
            - name: productName
              string: ca.test.data.flow032.product_P1_Name

      # Add to cart
      - executeFunction:
          name: ca.test.functions.PDPpage.addToCart
          params:
            - name: quantity
              string: ca.test.data.flow032.product_P1_Quantity

      # close
      - executeFunction:
          name: ca.test.functions.PDPpage.closePDPpage

      #clear search box
      - executeFunction:
          name: ca.test.functions.productSearch.clearSearch

      # Product Search using sku
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:           
            - name: product
              string: ca.test.data.flow032.product_P2_sku

      # Add to cart
      - executeFunction:
          name: ca.test.functions.productSearch.addToCart
          params:
            - name: productName
              string: ca.test.data.flow032.product_P2_Name
            - name: quantity
              string: ca.test.data.flow032.product_P2_Quantity

      # Asserting cart quantity
      - executeFunction:
           name: ca.test.functions.cart.assertCartQuantityIos
           params:
            - name: cartQuantity
              string: ca.test.data.flow032.cartQuantity
      
      # Asserting cart total
      - executeFunction:
          name: ca.test.functions.cart.assertCartTotal
          params:
            - name: subTotal
              string: ca.test.data.flow032.cartValue

# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      # Navigate to cart
      - executeFunction:
          name: ca.test.functions.global.navigation.goToCartIos

      # Book a delivery slot
      - executeFunction:
          name: ca.test.functions.cart.deliverySlotIos
      
      # Cart fulfillment verification
      - executeFunction:
          name: ca.test.functions.cart.deliveryFromStore.verifyCard

      # - executeFunction:
      #     name: ca.test.functions.cart.verifyProductDetails
      #     params:
      #       - name: productName
      #         string: ca.test.data.flow032.product_P1_itemName
      #       - name: productPrice
      #         string: ca.test.data.flow032.product_P1.itemPrice
      #       - name: productQty
      #         string: ca.test.data.flow032.product_P1_Quantity
            
      # - executeFunction:
      #     name: ca.test.functions.cart.verifyProductDetails
      #     params:
      #       - name: productName
      #         string: ca.test.data.flow032.product_P2_itemName
      #       - name: productPrice
      #         string: ca.test.data.flow032.product_P2.itemPrice
      #       - name: productQty
      #         string: ca.test.data.flow032.product_P2_Quantity

      # Verify cart subTotal
      - executeFunction:
          name: ca.test.functions.cart.cartSubTotal
          params: 
            - name: subTotal
              string: ca.test.data.flow032.cartSubTotal

      # - executeNode:
      #     file: ca/test/helpers/sanitizePriceIos.js
      #     args:
      #       - value: ${slotPrice}
      #     getResponse:
      #       storeIn: clearPrice
      # - log: ${clearPrice}

      # # Verify cart grocery delivery fee 
      # - executeFunction:
      #     name: ca.test.functions.cart.validateGroceryDeliveryFees
      #     params: 
      #       - name: grocery_delivery
      #         string: ${clearPrice}

      # - executeFunction:
      #     name: ca.test.functions.cart.taxCalculation
      #     params:
      #       - name: subTotal
      #         string: ca.test.data.flow032.cartSubTotal
      #       - name: slotPrice
      #         string: ${clearPrice}
      #       - name: tax
      #         string: ca.test.data.flow032.cart.hstPercentage
            
      # # Verify cart Taxes
      # - executeFunction:
      #     name: ca.test.functions.cart.cartTaxes
      #     params: 
      #       - name: taxes
      #         string: ${tax_values}

      # # Verify cart HST
      # - executeFunction:
      #     name: ca.test.functions.cart.hstTaxAssertion
      #     params: 
      #       - name: hst_percentage
      #         string: ca.test.data.flow032.cart.hstPercentage
      #       - name: hst_tax_value
      #         string: ${tax_values}

      # - executeFunction:
      #     name: ca.test.functions.cart.estimatedTotalCalculation
      #     params:
      #       - name: tax
      #         string: ${tax_values}
      #       - name: subTotal
      #         string: ca.test.data.flow032.cartSubTotal
      #       - name: slotPrice
      #         string: ${clearPrice}

      # # Verify cart estimated total
      # - executeFunction:
      #     name: ca.test.functions.cart.cartEstimatedTotal
      #     params: 
      #       - name: estimate_total
      #         string: ${estimate_total}

      # Proceed to Checkout
      - executeFunction:
          name: ca.test.functions.cart.proceedToCheckOutIos

      # Asserting Driver Tip
      - executeFunction:
          name: ca.test.functions.checkout.DriverTip.zeroTip

      # Validating payment card
      - executeFunction:
          name: ca.test.functions.checkOut.paymentOptionsIos       

# Paypal Payment
      - executeFunction:
          name: ca.test.functions.checkOut.paypalExistingUserExperience
          params:
            - name: userName
              string: sb-80yfm408689@personal.example.com
            - name: payPalPassword
              string: h@EW8+Ew
      
      # Adding mobile number
      - executeFunction:
          name: ca.test.functions.checkout.phoneNumber.enterContactNumber

      # Promo Code Check
      - executeFunction:
          name: ca.test.functions.checkOut.promoCodeAvailabilityCheck                 

      #  # Checkout page sub total assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.subTotalValidation
      #     params:
      #       - name: subTotal
      #         string: ca.test.data.flow032.cartSubTotal

      # # Verify cart grocery delivery fee
      # - executeFunction:
      #     name: ca.test.functions.checkOut.validateGroceryDeliveryFees
      #     params: 
      #       - name: grocery_delivery
      #         string: ${clearPrice}

      # # Checkout page tax assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.taxesAssertion
      #     params:
      #       - name: total_tax
      #         string: ${tax_values}
              
      # # Checkout page hst assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.hstAssertion
      #     params:
      #       - name: hst_percentage
      #         string: ca.test.data.flow032.hstPercentage
      #       - name: hst_tax_value
      #         string: ${tax_values}

      # # Checkout page estimate total assertion
      # - executeFunction:  
      #     name: ca.test.functions.checkOut.estimateTotalAssertion
      #     params:
      #       - name: estimate_total
      #         string: ${estimate_total}

      # Order placement 
      - executeFunction:
          name: ca.test.functions.checkOut.PlacingOrder

# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *          

      # Order Confirmation screen validation
      - executeFunction:
          name: ca.test.functions.checkOut.orderConfirmationValidationIos   

      # Order ID Assertion From Order Confirmation
      - executeFunction:
          name: ca.test.functions.checkOut.orderConformation.orderIdExtraction

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    flow:
      - executeFunction:
          name: ca.test.functions.utils.afterSteps     