#-------------------------------------------------------------------------------------------------------------------
# Amend Order : Decrease Qty which falls below threshold and validate the error message
# Scenario covered:
# Navigate onboarding to home
# Navigate to sign in page and signin
# Checking and selecting Store in HP
# Placing order using astro api
# Navigate to account page and purchase page
# Decrease Qty and validate OD
# Order cancellation
#-------------------------------------------------------------------------------------------------------------------
general:
  platform: ios
  tags: flow-042, ca_poj, fr_poj, Post-Transction-English-iOS-Flow, Post-Transction-French-iOS-Flow
  inherit:
    filesRunAll:
      - ca-errors-helpers.yaml

scenarios:

  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: Execute before steps

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
  - name: Main
    flow:

      - log: Start Main

      # Get timestamp
      - getTimestamp:
          asDate: false
          storeIn: timestamp
          
      #temporary#
      - executeFunction:
          name: ca.test.functions.utils.avoidTestPopup

       # Navigate onboarding to home
      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateiosOnboardingToHome
      
      # Verifying home screen
      - executeFunction:
          name: ca.test.functions.home.headerElements.validateIosHomePage
      - executeFunction:    
          name: ca.test.functions.home.livingDesign.validateIosBottomNavigation

      # Creating account using astro
      - executeFunction:
          name: ca.test.functions.astro.createAccount
          params:
            - name: payLoad
              string: ca.test.data.flow042.astroRequestBody     

       # Navigate to sign in page
      - executeFunction:
          name: ca.test.functions.home.navigateHomeToSignInIos

      - executeFunction:
          name: ca.test.functions.global.authentication.signInAccountIos
          params:
            - name: email
              string: ca.test.data.flow042.emailios
            - name: password
              string: ca.test.data.flow042.passwordios
            - name: validateSignInPage
              string: true

      - executeFunction:
          name: ca.test.functions.home.verifyUserNameInTopleft

      - executeFunction:
          name: ca.test.functions.utils.terminateApp

      - executeFunction:
          name: ca.test.functions.utils.relaunchApp     

      # Checking and selecting Store
      - executeFunction:
          name: ca.test.functions.home.storeSelect
          params:
            - name: Zip_Code
              string: ca.test.data.onboarding.zipcode
            - name: storeName
              string: ca.test.data.onboarding.storeName      

      # Placing order using astro api
      - executeFunction:
          name: ca.test.functions.astro.placeOrder.scheduledPickup
          params:
            - name: email
              string: ca.test.data.flow042.emailios
            - name: password
              string: ca.test.data.flow042.passwordios
            - name: payLoad
              string: ca.test.data.flow042.ios.astroOrderBody

# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *              
          
      # Navigate to account page
      - executeFunction:
          name: ca.test.functions.home.navigateHomeToAccountPage
    
      # Go to purchase page
      - executeFunction:
          name: ca.test.functions.accountPage.purchaseHistoryButtonIos

      # Workaround for app stuck in purchase history
      #Terminate and relaunch the app
      - executeFunction:
         name: ca.test.functions.utils.terminateApp
      - executeFunction:
          name: ca.test.functions.utils.relaunchApp   

      # Navigate to account page
      - executeFunction:
          name: ca.test.functions.home.navigateHomeToAccountPage
    
      # Go to purchase page
      - executeFunction:
          name: ca.test.functions.accountPage.purchaseHistoryButtonIos      

#       # # Validating purchase history page 
#       # - executeFunction:
#       #     name: ca.test.functions.purchaseHistoryPageValidationIos

# Workaround for app stuck in purchase history
      #Terminate and relaunch the app
      - executeFunction:
         name: ca.test.functions.utils.terminateApp
      - executeFunction:
          name: ca.test.functions.utils.relaunchApp 

      # Navigate to account page
      - executeFunction:
          name: ca.test.functions.home.navigateHomeToAccountPage
    
      # Go to purchase page
      - executeFunction:
          name: ca.test.functions.accountPage.purchaseHistoryButtonIos      

      # Purchase history page to view Details
      - executeFunction:
          name: ca.test.functions.purchaseHistory.viewDetailsButtonIos
      
      # # Order Id assertion                     
      # - executeFunction:
      #     name: ca.test.functions.purchaseHistory.viewDetails.orderIdAssertion
      
      # # Address Assertion
      # - executeFunction:
      #     name: ca.test.functions.purchaseHistory.viewDetails.addressAssertion
      #     params:
      #       - name: addressLine
      #         string: ca.test.data.flow042.addressLine 
      
      # # Cost Assertion
      # - executeFunction:
      #     name: ca.test.functions.purchaseHistory.viewDetails.costAssertion
      #     params:
      #       - name: subTotalAstro
      #         string: ca.test.data.flow042.subtotal
      #       - name: totalTax
      #         string: ca.test.data.flow042.Taxes
      #       - name: estimate_total
      #         string: ca.test.data.flow042.Total  

      # Decrease Qty and validate OD
      - executeFunction:
          name: ca.test.functions.purchaseHistory.viewDetails.amendPageQtyDecreaseIos
          params:
            - name: productprice042
              string: ca.test.data.flow042iPp.Total 

      # Validation of error message  is pending due to technical issue page showing up instead of error message.

      # Navigate back
      - executeFunction:
          name: ca.test.functions.purchaseHistory.navigationBack

      # Order cancellation
      - executeFunction:
          name: ca.test.functions.purchaseHistory.orderCancellation    

      - storeIn:
          key: testStatus
          value: passed

  - name: After
    after: true
    flow:
      - log:
          message: flow-042
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.utils.afterSteps