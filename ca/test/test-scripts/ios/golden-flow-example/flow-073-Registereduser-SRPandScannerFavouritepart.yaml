#---------steps-------------
# Sign in as reg user
# add a GM product (non fav) -directly add to cart
# search a GO item 
# add the item to fav through SRP
# search a GO item (change to multivariant item in future)
# add the item to fav through SRP
# naviagte to account page, go to fav page
# click Add all to cart
# go to cart, select devlivery from store for GO and select shipping for GM
# place order

#pending - scanner -> add a multivariant product through scanner
#---------------------------
general:
  platform: ios
  tags: flow-073, ca_authpoc, fr_ca, E2E-iOS-English, E2E-iOS-French
  inherit:
    filesRunAll:
      - ca-errors-helpers.yaml

scenarios:
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: Execute before steps

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

  - name: Main
    flow:
      - log: Start Main

      # Get TimeStamp
      - getTimestamp:
          asDate: false
          storeIn: timestamp
          
      #temporary#
      - executeFunction:
          name: ca.test.functions.utils.avoidTestPopup

      # Navigate onboarding to home
      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateiosOnboardingToHome
          params:
            - name: ZIP_CODE
              string: ca.test.data.flow073.onboardingZipcode
      
      # Verifying home screen
      - executeFunction:
          name: ca.test.functions.home.headerElements.validateIosHomePage

      - executeFunction:    
          name: ca.test.functions.home.livingDesign.validateIosBottomNavigation

      # Creating account using astro
      - executeFunction:
          name: ca.test.functions.apis.createAccount 

      # Home to Sign In
      - executeFunction:
          name: ca.test.functions.home.signIn  
      
      # Sign In to the account
      - executeFunction:
          name: ca.test.functions.loginPage.userLogin
          params:
            - name: email
              string: ${emailId}
            - name: password
              string: ${password}

      - executeFunction:
          name: ca.test.functions.utils.terminateApp

      - executeFunction:
          name: ca.test.functions.utils.relaunchApp
    
      # Checking and selecting Store
      - executeFunction:
          name: ca.test.functions.home.storeSelect
          params:
            - name: Zip_Code
              string: ca.test.data.flow073.onboardingZipcode
            - name: storeName
              string: ca.test.data.flow073.storeName

      # Selecting address at home page
      - executeFunction:
          name: ca.test.functions.home.changeLocationLink
          params:
            - name: address
              string: ca.test.data.flow073.deliveryAddress
          
      # Product Search
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:            
            - name: product
              string: ca.test.data.flow073.product_1_sku

      # # SRP Page Validation
      # - executeFunction:
      #     name: ca.test.functions.productSearch.srpPageValidaion

      # Add to cart
      - executeFunction:
          name: ca.test.functions.productSearch.addToCart
          params:
            - name: productName
              string: ca.test.data.flow073.product_1_name
            - name: quantity
              string: ca.test.data.flow073.product_1_Qty

      #clear search box
      - executeFunction:
          name: ca.test.functions.productSearch.clearSearch

      # Product Search
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:           
            - name: product
              string: ca.test.data.flow073.product_2_sku
          
      # Adding product to favourites
      - executeFunction:
          name: ca.test.functions.searchPage.favouritesIcon
          params:
            - name: productName
              string: ca.test.data.flow073.product_2_name

      # Navigating to My Items page
      - executeFunction:
          name: ca.test.functions.srpPage.navigateToMyItems
          params:
            - name: myItemsTab
              string: ca.test.data.myItems.favourites

      # Adding 1st product based on product name to cart from Favourite page
      - executeFunction:
          name: ca.test.functions.myItems.favouriteAddToCart
          params:
            - name: productName
              string: ca.test.data.flow073.product_2_name

      - executeFunction:
          name: ca.test.functions.favouritePage.removeItem
          params:
            - name: productName
              string: ca.test.data.flow073.product_2_name

      # Validate cart quantity
      - executeFunction:
           name: ca.test.functions.cart.assertCartQuantity
           params:
            - name: cartQuantity
              string: ca.test.data.flow073.cartQuantity

      # Asserting cart total
      - executeFunction:
          name: ca.test.functions.cart.assertCartTotal
          params:
            - name: subTotal
              string: ca.test.data.flow073.cartSubTotal

# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      # Navigate to cart
      - executeFunction:
          name: ca.test.functions.global.navigation.goToCartIos

      # Book a delivery slot
      - executeFunction:
          name: ca.test.functions.cart.deliverySlotIos
      
      # Cart fulfillment verification
      - executeFunction:
          name: ca.test.functions.cart.deliveryFromStore.verifyCard

      - executeFunction:
          name: ca.test.functions.cart.verifyProductDetails.withStrikOutPrice
          params:
            - name: productName
              string: ca.test.data.flow073.product_1_name
            - name: productPrice
              string: ca.test.data.flow073.product_p1.productPrice
            - name: productQty
              string: ca.test.data.flow073.product_1_Qty
            - name: strikeOutPrice
              string: ca.test.data.flow073.product_p1.strikeOutPrice
            - name: savePrice
              string: ca.test.data.flow073.product_p1.savedPrice

      - executeFunction:
          name: ca.test.functions.cart.verifyProductDetails
          params:
            - name: productName
              string: ca.test.data.flow073.product_2_name
            - name: productPrice
              string: ca.test.data.flow073.product_p2.itemPrice
            - name: productQty
              string: ca.test.data.flow073.product_2_Qty

      # Verify cart subTotal
      - executeFunction:
          name: ca.test.functions.cartXo.subTotalAssertionWithSaving
          params:
            - name: oldSubTotal
              string: ca.test.android.data.flow073.subTotalBeforeSaving
            - name: savings
              string: ca.test.android.data.flow073.subTotalSaving
            - name: newSubTotal
              string: ca.test.data.flow073.subTotalAfterSaving

      - executeNode:
          file: ca/test/helpers/sanitizePriceIos.js
          args:
            - value: ${slotPrice}
          getResponse:
            storeIn: clearPrice
      - log: ${clearPrice}

      # Verify cart grocery delivery fee 
      - executeFunction:
          name: ca.test.functions.cart.validateGroceryDeliveryFees
          params: 
            - name: grocery_delivery
              string: ${clearPrice}

      - executeFunction:
          name: ca.test.functions.cart.taxCalculation
          params:
            - name: subTotal
              string: ca.test.data.flow073.subTotalAfterSaving
            - name: slotPrice
              string: ${clearPrice}
            - name: tax
              string: ca.test.data.flow073.cart.hstPercentage
            
      # Verify cart Taxes
      - executeFunction:
          name: ca.test.functions.cart.cartTaxes
          params: 
            - name: taxes
              string: ${tax_values}

      # Verify cart HST
      - executeFunction:
          name: ca.test.functions.cart.hstTaxAssertion
          params: 
            - name: hst_percentage
              string: ca.test.data.flow032.cart.hstPercentage
            - name: hst_tax_value
              string: ${tax_values}

      - executeFunction:
          name: ca.test.functions.cart.estimatedTotalCalculation
          params:
            - name: tax
              string: ${tax_values}
            - name: subTotal
              string: ca.test.data.flow073.subTotalAfterSaving
            - name: slotPrice
              string: ${clearPrice}

      # Verify cart estimated total
      - executeFunction:
          name: ca.test.functions.cart.cartEstimatedTotal
          params: 
            - name: estimate_total
              string: ${estimate_total}

      # Proceed to Checkout
      - executeFunction:
          name: ca.test.functions.cart.proceedToCheckOutIos

      # Asserting Driver Tip
      - executeFunction:
          name: ca.test.functions.checkout.DriverTip.zeroTip

      # Validating payment card
      - executeFunction:
          name: ca.test.functions.checkOut.paymentOptionsIos       

      # Paypal Payment
      - executeFunction:
          name: ca.test.functions.checkOut.paypalExistingUserExperience
          params:
            - name: userName
              string: ca.test.data.paypal.userName
            - name: payPalPassword
              string: ca.test.data.paypal.password
      
      # Adding mobile number
      - executeFunction:
          name: ca.test.functions.checkout.phoneNumber.enterContactNumber                

      #  # Checkout page sub total assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.subTotalValidation
      #     params:
      #       - name: subTotal
      #         string: ca.test.data.flow032.cartSubTotal

      # # Verify cart grocery delivery fee
      # - executeFunction:
      #     name: ca.test.functions.checkOut.validateGroceryDeliveryFees
      #     params: 
      #       - name: grocery_delivery
      #         string: ${clearPrice}

      # # Checkout page tax assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.taxesAssertion
      #     params:
      #       - name: total_tax
      #         string: ${tax_values}
              
      # # Checkout page hst assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.hstAssertion
      #     params:
      #       - name: hst_percentage
      #         string: ca.test.data.flow032.hstPercentage
      #       - name: hst_tax_value
      #         string: ${tax_values}

      # # Checkout page estimate total assertion
      # - executeFunction:  
      #     name: ca.test.functions.checkOut.estimateTotalAssertion
      #     params:
      #       - name: estimate_total
      #         string: ${estimate_total}

      # Order placement 
      - executeFunction:
          name: ca.test.functions.checkOut.PlacingOrder

# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *          

      # # Order Confirmation screen validation
      # - executeFunction:
      #     name: ca.test.functions.checkOut.orderConfirmationValidationIos1 
      #     params:
      #       - name: estimate_total
      #         string: ${estimate_total}
      #       - name: addressFirstLine
      #         string: ca.test.data.orderConfirmation.flowIos.Address  

      # Order Confirmation screen validation
      - executeFunction:
          name: ca.test.functions.checkOut.orderConfirmationValidationIos 

      # # Continue Shopping
      # - executeFunction:
      #     name: ca.test.functions.checkOut.continueShopping     

      # # Navigate to account page
      # - executeFunction:
      #     name: ca.test.functions.home.navigateHomeToAccountPage
    
      # # Go to purchase page
      # - executeFunction:
      #     name: ca.test.functions.accountPage.purchaseHistoryButtonIos

      # # Purchase history page to view Details
      # - executeFunction:
      #     name: ca.test.functions.purchaseHistory.viewDetailsButtonIos

      # # Order cancellation
      # - executeFunction:
      #     name: ca.test.functions.purchaseHistory.orderCancellation  

      # # Order cancellation from home
      # - executeFunction:
      #     name: ca.test.functions.purchaseHistory.orderCancellationHome                  

      - storeIn:
          key: testStatus
          value: passed
  
  - name: After
    after: true
    flow:
      - executeFunction:
          name: ca.test.functions.utils.afterSteps     

           
          
                    