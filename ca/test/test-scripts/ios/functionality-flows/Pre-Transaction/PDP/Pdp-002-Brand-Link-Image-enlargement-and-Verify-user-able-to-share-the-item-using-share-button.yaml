general:
  platform: ios
  tags: pdp-002, ca-function-run
  inherit:
    filesRunAll:
      - ca-errors-helpers.yaml

scenarios:
  - name: Before
    before: false
    endTestOnFailure: false
    flow:
      - log: Execute before steps 

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
  
      # Onboarding to app and Sign In as registered user 
      - log:
          message: Onboarding to app and Sign In as registered user
          color: CYAN_BOLD_BRIGHT
          
      # Navigate onboarding to home
      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateiosOnboardingToHome

      # Navigate to sign in page
      - executeFunction:
          name: ca.test.functions.home.navigateHomeToSignInIos 
   
      # Login us registered user
      - executeFunction:
          name: ca.test.functions.global.authentication.signInAccountIos
          params:
            - name: email
              string: ca.test.data.pdp002Ios.emailId
            - name: password
              string: ca.test.data.functional.password  
    
      # NAvigate to home page
      - executeFunction:
          name: ca.test.functions.accountPage.navigateToShop
      - log:
           message: Signed In as registered user!
           color: YELLOW 
           
      # Clear Cart
      - executeFunction:
          name:  ca.test.functions.cart.removeCartItems      
      
    #   - executeFunction: -- Function with Array for OOS 
    #       name: ca.test.function.productSearch.UsingDynamicProductArrayList
    #       params:
    #         - name: ProductArray
    #           string: ca.test.data.productArray.productGM
    #         - name: Cartqty
    #           string: 1
    
    # Search a product
      - log:
          message: Search a GM product
          color: CYAN_BOLD_BRIGHT
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:
            - name: product
              string: ca.test.data.product_1_sku

      # Extracting first product name from SRP
      - log: 
          message: Extracting first product name from SRP.
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.productSearch.extractingFirstProductName

      - log: 
          message: Storing product name to a variable.
          color: GREEN_BOLD
      - storeIn:
          key: gm_item
          value: ${productTitle}

      # Navigating to PDP page 
      - log: 
          message: Navigating to PDP page of the product.
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.productSearch.prod.srpToPdpPage
          params:
            - name: productName
              string: ${gm_item}

      # Pdp product header validation
      - log:
          message: Validating product header tile in PDP.
          color: GREEN_BOLD                             
      - executeFunction:
          name: ca.test.functions.pdp.productHeaderContainer  

       #close PDP page
      - executeFunction:
          name: ca.test.functions.PDPpage.closePDPpage
      - log:
          message: Brand Link, Image enlargement and Verify user able to share the item using share button
          color: CYAN_BOLD_BRIGHT  

      # Validate weight info is not shown for eaches product 
      - log:
          message: Validate weight info is not shown for eaches product 
          color: CYAN_BOLD_BRIGHT

      # Product Search
      - executeFunction:
          name: ca.test.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: ca.test.data.pdp002.GO
     
      #validate the label in PDP page
      - executeFunction:
          name: ca.test.functions.pdp.weightValidation

      #close PDP page
      - executeFunction:
          name: ca.test.functions.PDPpage.closePDPpage
      - log:
            message: Validated weight info is not shown for eaches product !
            color: GREEN_BOLD_BRIGHT 
      
      # Search a product
      - executeFunction:
          name: ca.functions.searchPage.searchItemArray
          params:
            - name: items
              string: ca.test.data.pdp003.gmItemArray
            - name: quantity
              string: ca.test.data.pdp003.gmItemQty
            - name: checkNonPromotionBadges
              string: true          

      - log: 
          message: Storing product name to a variable.
          color: GREEN_BOLD
      - storeIn:
          key: gm_item_1
          value: ${productTitle}

      # Navigating to PDP page
      - log: 
          message: Navigating to PDP page of the product.
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.productSearch.prod.srpToPdpPage
      
      # Close PDP
      - log: 
          message: Closing PDP page.
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.PDPpage.closePDPpage  

      #clear search box
      - executeFunction:
          name: ca.test.functions.productSearch.clearSearch     

      # Search a Product
      - log: 
          message: Searching for a GO product
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:
            - name: product
              string: ca.test.data.product_3_sku

      # Navigating to PDP page
      - log: 
          message: Navigating to PDP page of the product.
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.productSearch.prod.srpToPdpPage
          params:
            - name: productName
              string: ca.test.data.product_3_name

      # Close PDP
      - log: 
          message: Closing PDP page.
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.PDPpage.closePDPpage 

      # Search a product
      - log: 
          message: Searching for a GM product with age restriction
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.global.navigation.goToDeepLink
          params:
            - name: deepLinkUrl
              string: ca.test.data.pdp002.ageRestrictedItemSku

      - executeFunction:
          name: ca.test.functions.pdp.productHeaderContainer  

    # more Details Validation    
      - executeFunction:
          name: ca.test.functions.pdp.MoreDetails
      - executeFunction:
          name: ca.test.functions.pdp.MoreDetailsIos.AgeRestricted      

    # Closing PDP page
      - log: 
          message: Closing PDP page.
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.PDPpage.closePDPpage  
          
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      - storeIn:
          key: testStatus
          value: passed
  
  - name: After
    after: true
    flow:
      - log:
          message: pdp-002-ios
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.utils.afterSteps            