#-------------------------------------------------------------------------------------------------------------------
# POX: 002
# User: Amend Order, Registered User
# Order Type: GO
# Fulfillment: SCHEDULED_PICKUP
# Payment Type: CC
# Experience: Decrease Qty
# Validation: Validate OD
# Developed BY: sureshkumar Peethambaram
# Remark:  
#-------------------------------------------------------------------------------------------------------------------

general:
  platform: ios
  tags: POX-002,ca_POX,en_POX
  inherit:
    filesRunAll:
      - errors-helpers.yaml

scenarios:
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: Execute before steps

      # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
  # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
  - name: Main
    flow:

      - log: Start Main

      # Get TimeStamp
      - getTimestamp:
          asDate: false
          storeIn: timestamp

      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateiosOnboardingToHomeparamZip
          params:
            - name: ZIP_CODE
              string: ca.test.data.pox002.onboardingZipCode

     
      # Creating account using astro
      - executeFunction:
          name: ca.test.functions.astro.createAccount
          params:
            - name: payLoad
              string: ca.test.data.pox002.astroRequestBody1
       # Verifying home screen
      - executeFunction:
          name: ca.test.functions.home.headerElements.validateIosHomePage

      # # Verifying home screen bottom ribbon
      # - executeFunction:
      #     name: ca.test.functions.home.livingDesign.validateIosBottomNavigation


      # # Navigate to Account Tab
      # - executeFunction:
      #     name: ca.test.functions.global.navigation.goToAccountFromTabBarIos
                #ca.test.functions.home.navigateHomeToAccountPage
#      # Navigate to Account Tab
#      - executeFunction:
#          name: ca.test.functions.global.navigation.goToAccountFromTabBar
#
#      # Account page validation after login
#      - executeFunction:
#          name: ca.test.functions.accountPage.accountPageBeforeLogin

      # Placing order using astro api
      - executeFunction:
          name: ca.test.functions.astro.placeOrder.scheduledPickup
          params:
            - name: email
              string: ca.test.data.pox002.emailId
            - name: password
              string: ca.test.data.pox002.password
            - name: payLoad
              string: ca.test.data.pox002.astroOrderBody
      # Login to account
      - executeFunction:
          name: ca.test.functions.home.signInIos
      
      # Login to account
      - executeFunction:
          name: ca.test.functions.global.authentication.signInAccountIos
          params:
            - name: email
              string: ca.test.data.pox002.emailId
            - name: password
              string: ca.test.data.pox002.password
           
       # Navigate to Account Tab
      - executeFunction:
          name: ca.test.functions.global.navigation.goToAccountFromTabBarIos

      # # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_Post order experience'
      # # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
         
      # Go to purchase page
      - executeFunction:
          name: ca.test.functions.accountPage.purchaseHistoryButtonIos

      # # Validating purchase history page
      # - executeFunction:
      #     name: ca.test.functions.purchaseHistoryPageValidationIos
      # Workaround for app stuck in purchase history
      #Terminate and relaunch the app
      - executeFunction:
         name: ca.test.functions.utils.terminateApp
      - executeFunction:
          name: ca.test.functions.utils.relaunchApp
      # Navigate to account page
      - executeFunction:
          name: ca.test.functions.home.navigateHomeToAccountPage
      # Go to purchase page
      - executeFunction:
          name: ca.test.functions.accountPage.purchaseHistoryButtonIos

      # Purchase history page to view Details
      # - executeFunction:
      #     name: ca.test.functions.purchaseHistory.viewDetailsButtonIos
      - executeFunction:
          name: ca.test.mappings.purchaseHistory.viewDetails.placedOrder

#      # Navigating to edit order button
#      - executeFunction:
#          name: ca.test.functions.purchaseHistory.viewDetails.editOrderButton
#
#      - executeFunction:
#          name: ca.test.functions.purchaseHistory.viewDetails.amendQuantity
#          params:
#            - name: amendQuantity
#              string: ca.test.data.pox002.amendType
#            - name: Quantity
#              string: ca.test.data.pox002.amendQuantity
      - executeFunction:
          name: ca.test.functions.amendOrder.decreaseQty
     
      # status
      - storeIn:
          key: testStatus
          value: passed

  # After Scenarios
  - name: After
    after: true
    flow:
      - log:
          message: POX-002
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.utils.afterSteps
