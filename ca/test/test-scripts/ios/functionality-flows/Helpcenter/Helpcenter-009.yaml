general:
  platform: ios
  tags: helpcenter

scenarios:
  #--------------------------------------------------------------------------------------------------------------------
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: TEST START - Help Regression - 009 - BEFORE SCENARIO
      # - executeFunction:
      #     name: ca.test.functions.welcome.assertPageDisplayed

  #--------------------------------------------------------------------------------------------------------------------
  - name: Validate the search clear option
    flow:
      - log: TEST START - Help Regression - 009

      # Navigate Onboarding Screens to Home Screen...
      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateiosOnboardingToHome
              
      - executeFunction:
          name: ca.test.functions.helpCentre.navigateToHelpCentre

      - executeFunction:
         name: ca.test.functions.helpCentre.search
         params:
           - name: helpsearch
             string: "jdfhjfhdjfdj"

      - executeFunction:
         name: ca.test.functions.helpCentre.clickCancel

      - executeFunction:
         name: ca.test.functions.helpCentre.verifyHelpCentreScreenElements

      - executeFunction:
         name: ca.test.functions.helpCentre.search
         params:
           - name: helpsearch
             string: "jdfhjfhdjfdj"

      - executeFunction:
         name: ca.test.functions.helpCentre.clickClearText

      - executeFunction:
         name: ca.test.functions.helpCentre.getSearchText

      - if:
          condition: "${searchText} == 'jdfhjfhdjfdj'"
          then:
            - failTest:
                message: "search text is not clearing"