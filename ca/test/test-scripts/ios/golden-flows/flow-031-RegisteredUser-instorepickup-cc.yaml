#-------------------------------------------------------------------------------------------------------------------
# Registered User : Place a In-store Pick up Order Payment Type : CC. As part of checkout flow validate "Forgot something on your list" / Don't forget your bread (From cart to checkout page)  
# Scenario covered:
# Navigate onboarding to home
# clear cart through astro 
# Navigate to sign in page and signin
# Checking and selecting Store in HP
# Search a Product and add through srp
# Navigate to cart and validate product details and estimate total in cart
# Proceed to Checkout
# Reserve a time after checkout
# Validate fulfillment type in review order page, mobile number, GO pickup fee and estimate total
# Place the order and Validate Order Confirmation page
# oder cancellation through account page
#-------------------------------------------------------------------------------------------------------------------
general:
  platform: ios
  tags: flow-031, ca_authpoc, test_run_ios, fr_ca, E2E-iOS-English, E2E-iOS-French
  inherit:
    filesRunAll:
      - ca-errors-helpers.yaml

scenarios:
  - name: Before
    before: true
    endTestOnFailure: true
    flow:
      - log: Execute before steps

# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
      - log: 'R2_SUBFLOW_PRE_TRANSACTION'
# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
  - name: Main
    flow:
      - log: Start Main

      # Get TimeStamp
      - getTimestamp:
          asDate: false
          storeIn: timestamp
          
      #temporary#
      - executeFunction:
          name: ca.test.functions.utils.avoidTestPopup

      # Navigate onboarding to home
      - executeFunction:
          name: ca.test.functions.global.onboarding.navigateiosOnboardingToHomeparamZip
          params:
            - name: ZIP_CODE
              string: ca.test.data.flow031.zipCode

      # Creating account using astro
      - executeFunction:
          name: ca.test.functions.astro.createAccount
          params:
            - name: payLoad
              string: ca.test.data.flow031.astroRequestBody

      # Navigate to sign in page
      - executeFunction:
          name: ca.test.functions.home.signIn 

      - executeFunction:
          name: ca.test.functions.loginPage.userLogin
          params:
            - name: email
              string: ca.test.data.flow031.email
            - name: password
              string: ca.test.data.flow031.password

      # # Validating home page customer greeting
      # - executeFunction:
      #     name: ca.test.functions.home.verifyUserNameHomePage
      #     params:
      #       - name: usergreeting
      #         string: ca.test.data.flow031.customerName
      
      - executeFunction:
          name: ca.test.functions.utils.terminateApp

      - executeFunction:
          name: ca.test.functions.utils.relaunchApp

      # Checking and selecting Store
      - executeFunction:
          name: ca.test.functions.home.storeSelect
          params:
            - name: Zip_Code
              string: ca.test.data.flow031.zipCode
            - name: storeName
              string: ca.test.data.flow031.storeName

      # Product Search
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:           
            - name: product
              string: ca.test.data.flow031.product_P1_sku #206498 #ca.test.data.flow001.product_p1_sku              

      # Add to cart
      - executeFunction:
          name: ca.test.functions.productSearch.addToCart
          params:
            - name: productName
              string: ca.test.data.flow031.product_P1_Name
            - name: quantity
              string: ca.test.data.flow031.product_P1_Quantity

      #clear search box
      - executeFunction:
          name: ca.test.functions.productSearch.clearSearch

      # Product Search
      - executeFunction:
          name: ca.test.functions.productSearch.searchAProduct
          params:           
            - name: product
              string: ca.test.data.flow031.product_P2_sku             

      # Add to cart
      - executeFunction:
          name: ca.test.functions.productSearch.addToCart
          params:
            - name: productName
              string: ca.test.data.flow031.product_P2_Name
            - name: quantity
              string: ca.test.data.flow031.product_P2_Quantity

      # Validate cart quantity
      - executeFunction:
          name: ca.test.functions.cart.assertCartQuantity
          params:
            - name: cartQuantity
              string: ca.test.data.flow031.cartQuantity   

      # Asserting cart total
      - executeFunction:
          name: ca.test.functions.cart.assertCartTotal
          params:
            - name: subTotal
              string: ca.test.data.flow031.cartSubTotal

# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *

      # Navigate to cart
      - executeFunction:
          name: ca.test.functions.global.navigation.goToCartIos

      # navigate to reserve time page
      - executeFunction:
          name: ca.test.functions.cart.reserveTimePage
      
      # Reserve time page validation
      - executeFunction:
          name: ca.test.functions.reserveTimePageValidation

      # navigating and changing store
      - executeFunction:
          name: ca.test.functions.reserveTime.changeStore
          params:
            - name: Zip_Code
              string: ca.test.data.flow031.zipCode
            - name: storeName
              string: ca.test.data.flow031.storeName
            - name: storeDisplayed
              string: ca.test.data.flow031.storeNameDisplay

      - executeFunction: 
          name: ca.test.functions.bookASlot.reserveTimePage.standardSlotSelection

      # Clicking book a slot button
      - executeFunction:
          name: ca.test.functions.bookASlot.reserveTimePage.bookASlotButton

      # Cart fulfillment verification
      - executeFunction:
          name: ca.test.functions.cart.pickup.verifyCardIos
      
      - executeFunction:
          name: ca.test.functions.cart.verifyProductDetails
          params:
            - name: productName
              string: ca.test.data.flow031.product_P1_Name
            - name: productPrice
              string: ca.test.data.flow031.product_P1.itemPrice
            - name: productQty
              string: ca.test.data.flow031.product_P1_Quantity

      - executeFunction:
          name: ca.test.functions.cart.verifyProductDetails
          params:
            - name: productName
              string: ca.test.data.flow031.product_P2_itemName
            - name: productPrice
              string: ca.test.data.flow031.product_P2.itemPrice
            - name: productQty
              string: ca.test.data.flow031.product_P2_Quantity

      # Commmented - due to the 1$ price difference issue:
      # # Verify cart subTotal
      # - executeFunction:
      #     name: ca.test.functions.cart.cartSubTotal
      #     params: 
      #       - name: subTotal
      #         string: ca.test.data.flow031.cartValue
      
      # - executeFunction:
      #     name: ca.test.functions.cart.groceryPickUp
      #     params:
      #       - name: slotPrice
      #         string: FREE

      # # Verify cart Taxes
      # - executeFunction:
      #     name: ca.test.functions.cart.cartTaxes
      #     params: 
      #       - name: taxes
      #         string: $5.79

      # # Asserting hst tax
      # - executeFunction:
      #     name: ca.test.functions.cart.hstTaxAssertion
      #     params:
      #       - name: hst_percentage
      #         string: ca.test.data.flow031.hstPercentage
      #       - name: hst_tax_value
      #         string: $5.79

      # # Verify cart estimated total
      # - executeFunction:
      #     name: ca.test.functions.cart.cartEstimatedTotal
      #     params: 
      #       - name: estimate_total
      #         string: $50.30
        
      # Proceed to Checkout
      - executeFunction:
          name: ca.test.functions.cart.checkout.proceedToCheckOutIos

      # CheckOut page order review validation
      - executeFunction:
          name: ca.test.functions.checkOut.validateCheckOutOrderCard
          params:
            - name: fulfillmentType
              string: ca.test.data.flow031.fulfillmentType

      # Adding mobile number
      - executeFunction:
          name: ca.test.functions.checkout.phoneNumber.enterContactNumber

      # # Checkout page sub total assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.subTotalValidation
      #     params:
      #       - name: subTotal
      #         string: ca.test.data.flow031.cartValue

      # # Verify checkout grocery pickup fee -- $1 shows FREE
      # - executeFunction:
      #     name: ca.test.functions.checkOut.subTotal.scheduleOrderFee
      #     params: 
      #       - name: scheduledOrderFeeText
      #         string: Grocery pickup fee
      #       - name: scheduledOrderFeeValue
      #         string: FREE

      # # Checkout page tax assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.taxesAssertion
      #     params:
      #       - name: total_tax
      #         string: $5.79
              
      # # Checkout page hst assertion
      # - executeFunction:
      #     name: ca.test.functions.checkOut.hstAssertion
      #     params:
      #       - name: hst_percentage
      #         string: 13%
      #       - name: hst_tax_value
      #         string: $5.79

      # # Checkout page estimate total assertion
      # - executeFunction:  
      #     name: ca.test.functions.checkOut.estimateTotalAssertion
      #     params:
      #       - name: estimate_total
      #         string: $50.30
            
      # Checkout
      - executeFunction:
          name: ca.test.functions.checkOut.PlacingOrder

# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
      - log: 'R2_SUBFLOW_POST_TRANSACTION'
# # * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *          

      # Order Confirmation screen validation
      - executeFunction:
          name: ca.test.functions.checkOut.orderConfirmationValidationIos

      - storeIn:
          key: testStatus
          value: passed
  
  - name: After
    after: true
    flow:
      - log:
          message: flow-031
          color: GREEN_BOLD
      - executeFunction:
          name: ca.test.functions.utils.afterSteps