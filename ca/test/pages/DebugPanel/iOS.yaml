functions:

  #--------------------------------------------------------------------------------------------------------------------

  # Description: This functions taps on the open button in deeplink popup
  # No Params
  - name: ca.test.functions.debugPanel.deepLink.tapOpenButton
    platform: ios
    flow:
      - click:
          identifier: ca.test.mappings.debugPanel.deepLink.openButton
      - log:
          message: "End functions: debugPanel.deepLink.tapOpenButton"
          color: BLUE