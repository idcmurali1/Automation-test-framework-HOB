functions:

  #--------------------------------------------------------------------------------------------------------------------

  # Description: This functions handle rating pop up if displayed
  # No Param
  - name: ca.test.functions.orderConfirmationPage.handleRatingPopUp
    flow:
      - if:
          identifier:
            present:
              - identifier: ca.test.mappings.orderConfirmationPage.cancelRating
          then:
            - click:
                identifier: ca.test.mappings.orderConfirmationPage.cancelRating
      - log:
          message: "End functions: orderConfirmationPage.handleRatingPopUp"
          color: GREEN_BOLD   

  #--------------------------------------------------------------------------------------------------------------------
